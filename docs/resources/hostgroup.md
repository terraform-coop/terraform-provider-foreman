---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "foreman_hostgroup Resource - terraform-provider-foreman"
subcategory: ""
description: |-
  
---

# foreman_hostgroup (Resource)



## Example Usage

```terraform
data "foreman_hostgroup" "datacenter" {
  name = "DC1"
}

data "foreman_domain" "dev" {
  name = "dev.example.com"
}

data "foreman_operatingsystem" "centos" {
  name = "CentOS 7.4"
}

resource "foreman_hostgroup" "app" {
  name        = "app"
  description = "Application servers"
  parent_id   = data.foreman_hostgroup.datacenter.id

  domain_id          = data.foreman_domain.dev.id
  operatingsystem_id = data.foreman_operatingsystem.centos.id

  root_pass = var.hostgroup_root_pass

  parameters = {
    role = "webserver"
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the host group

### Optional

- `architecture_id` (Number)
- `compute_profile_id` (Number)
- `compute_resource_id` (Number)
- `description` (String)
- `domain_id` (Number)
- `location_ids` (Set of Number) REPLACE locations with given ids Only the IDs listed here are tracked by Terraform; associations Foreman adds on its own (e.g. family-matched stock templates) are left untouched.
- `medium_id` (Number)
- `operatingsystem_id` (Number)
- `organization_ids` (Set of Number) REPLACE organizations with given ids. Only the IDs listed here are tracked by Terraform; associations Foreman adds on its own (e.g. family-matched stock templates) are left untouched.
- `parameters` (Map of String)
- `parent_id` (Number)
- `ptable_id` (Number)
- `puppet_ca_proxy_id` (Number)
- `puppet_proxy_id` (Number)
- `pxe_loader` (String)
- `realm_id` (Number)
- `root_pass` (String, Sensitive) Root password on provisioned hosts
- `subnet6_id` (Number)
- `subnet_id` (Number)

### Read-Only

- `id` (String) The ID of this resource.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import foreman_hostgroup.app <hostgroup_id>
```
