---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "foreman_katello_content_view Resource - terraform-provider-foreman"
subcategory: ""
description: |-
  
---

# foreman_katello_content_view (Resource)



## Example Usage

```terraform
# Look up an existing repository to include in the content view
data "foreman_katello_repository" "epel8" {
  name = "EPEL 8"
}

resource "foreman_katello_content_view" "epel8_cv" {
  name            = "EPEL 8 Content View"
  description     = "Content view containing the EPEL 8 repository"
  organization_id = 1

  repository_ids = [tonumber(data.foreman_katello_repository.epel8.id)]

  composite    = false
  auto_publish = false

  filters = [
    {
      name        = "exclude-testfilter"
      type        = "rpm"
      inclusion   = false
      description = "Excludes all packages named testfilter-*"

      rule = [
        {
          name = "testfilter-*"
        }
      ]
    }
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String)

### Optional

- `auto_publish` (Boolean)
- `component_ids` (List of Number)
- `composite` (Boolean)
- `content_host_count` (Number)
- `description` (String)
- `filtered` (Boolean)
- `filters` (Attributes List) Content view filters. Each filter can include rules for package, package group, erratum, or docker manifest filtering. (see [below for nested schema](#nestedatt--filters))
- `label` (String)
- `latest_version` (String)
- `latest_version_id` (Number)
- `organization_id` (Number)
- `repository_ids` (List of Number)
- `solve_dependencies` (Boolean)
- `version_count` (Number)

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String)
- `type` (String) Filter type: package, package_group, erratum, docker_manifest, or deb.

Optional:

- `description` (String)
- `inclusion` (Boolean) If true, the filter includes matching content. If false, it excludes matching content. Defaults to false.
- `rule` (Attributes List) Rules for this filter. (see [below for nested schema](#nestedatt--filters--rule))

Read-Only:

- `id` (Number)

<a id="nestedatt--filters--rule"></a>
### Nested Schema for `filters.rule`

Required:

- `name` (String)

Optional:

- `architecture` (String)

Read-Only:

- `id` (Number)

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import foreman_katello_content_view.epel8_cv <content_view_id>
```
