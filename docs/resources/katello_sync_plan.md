---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "foreman_katello_sync_plan Resource - terraform-provider-foreman"
subcategory: ""
description: |-
  
---

# foreman_katello_sync_plan (Resource)



## Example Usage

```terraform
resource "foreman_katello_sync_plan" "daily" {
  name        = "daily"
  description = "Daily sync of all subscribed products"
  interval    = "daily"
  enabled     = true

  # Run the first sync on Jan 1st 2024 at 05:10 UTC. When 'interval' is set,
  # only the time-of-day portion of 'sync_date' is used to schedule future runs.
  sync_date = "2024-01-01 05:10:00 +0000"

  # cron_expression = "*/5 * * * *" # alternative: run every 5 minutes
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String)

### Optional

- `cron_expression` (String)
- `description` (String)
- `enabled` (Boolean)
- `interval` (String)
- `sync_date` (String)

### Read-Only

- `id` (String) The ID of this resource.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import foreman_katello_sync_plan.daily <sync_plan_id>
```
